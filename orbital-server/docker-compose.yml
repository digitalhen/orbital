services:
  space-api:
    build: ./api
    volumes:
      - ./api/missions:/app/missions
    restart: unless-stopped

  orbital-website:
    build: ./website
    volumes:
      - ./downloads:/usr/share/nginx/html/orbital/downloads
    restart: unless-stopped
